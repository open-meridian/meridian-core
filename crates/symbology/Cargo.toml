[package]
name = "meridian-symbology"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "The identifier fallback order, in one place because two components must agree on it"

[dependencies]
meridian-domain.workspace = true
