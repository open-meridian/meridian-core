# Generated by tools/nats_permissions.py from the grant table. Do not edit.
#
# The grant table is the policy; this is the broker saying the same thing in
# its own language. Run `make nats-permissions` after changing grants.


authorization {
  users = [
    # The runtime's components. Broad until a component's
    # permissions are derived from the topic registry.
    { user: runtime, password: $MERIDIAN_NATS_RUNTIME, permissions: { publish: { allow: [">"] }, subscribe: { allow: [">"] } } }
    # custody-1, launched as custody
    { user: custody-1, password: $MERIDIAN_NATS_CUSTODY_1, permissions: { publish: { allow: ["platform.custody.custody-1.event.sync-status", "platform.kernel.command.record-holding", "platform.kernel.command.record-statement", "platform.reference.event.instrument-missing", "platform.reference.query.resolve-identifier", "platform.reference.query.resolve-instrument", "_INBOX.>"] }, subscribe: { allow: ["platform.reference.event.instrument-applied", "_INBOX.>"] }, allow_responses: true } }
    # custody-2, launched as custody
    { user: custody-2, password: $MERIDIAN_NATS_CUSTODY_2, permissions: { publish: { allow: ["platform.custody.custody-2.event.sync-status", "platform.kernel.command.record-holding", "platform.kernel.command.record-statement", "platform.reference.event.instrument-missing", "platform.reference.query.resolve-identifier", "platform.reference.query.resolve-instrument", "_INBOX.>"] }, subscribe: { allow: ["platform.reference.event.instrument-applied", "_INBOX.>"] }, allow_responses: true } }
    # dashboard-1, launched as dashboard
    { user: dashboard-1, password: $MERIDIAN_NATS_DASHBOARD_1, permissions: { publish: { allow: ["platform.kernel.query.list-custodial-positions", "platform.reference.query.resolve-identifier", "platform.reference.query.resolve-instrument", "_INBOX.>"] }, subscribe: { allow: ["platform.custody.*.event.sync-status", "platform.kernel.event.*", "platform.reference.event.*", "_INBOX.>"] }, allow_responses: true } }
  ]
}
