__pycache__/
*.pyc
.DS_Store
target/
dist/
build/
node_modules/
.venv/
*.log
.env
.env.*
target/

# A key generated by the note example, for a local demonstration only.
*.key

# Written by `make demo`: this deployment.
.demo/

# Written by `make test-store` when it fails.
.test-store.log
.interop.log
deploy/nats/dev.conf

# Written and removed by `make check-codegen`.
.codegen-scratch/

# Written and removed by `make e2e-cluster-k3d`: the throwaway cluster's
# credentials, which outlive it only if the run is interrupted.
.e2e-k3d.kubeconfig

# Made and replaced by `make test-directory`: a throwaway CA and the ldaps://
# test directory's certificate, trusted by nothing.
.e2e-ldap-tls/
