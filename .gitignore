__pycache__/
*.pyc
.DS_Store
target/
dist/
build/
node_modules/
.venv/
*.log
.env
.env.*
target/

# A key generated by the note example, for a local demonstration only.
*.key

# Written by `make demo`: this deployment.
.demo/

# Written by `make test-store` when it fails.
.test-store.log
.interop.log
deploy/nats/dev.conf

# Written and removed by `make check-codegen`.
.codegen-scratch/

# Both halves of the e2e Zitadel system user, made fresh by
# `make zitadel-system-user`. A committed public half whose private half is
# ignored is a pair that works only on the machine that made it.
e2e/zitadel/system-user.pub
