# Jobs that CI runs and `make ci-local` cannot.
#
# Format: job-id<TAB>reason. A bare job id is rejected; the reason is the point.
# Every entry here is a hole in "green locally means green in CI", so each one
# has to be worth the hole.
#
# The bar: a job belongs here only when reproducing it locally is impossible
# rather than merely inconvenient. Needing a credential, a hosted service, or a
# model is impossible. Being slow is not.

claude-review	Runs the review passes in REVIEW.md against the diff by invoking a model. Not reproducible locally: it needs credentials and produces a judgement, not a verdict. Advisory only -- ci-local is the required status check, this is not.
claude	The on-demand responder, triggered by an @claude mention on an issue or a review comment. Runs no checks and gates nothing; it exists so a human can ask for work in a thread. Nothing local corresponds to a mention.
publish	Pushes the runtime image to a registry. Not reproducible locally by design: it needs a registry credential, and a local target that pushed would mean every developer could publish from a laptop. It gates nothing; it runs only after `ci` has gone green on main, so nothing is published that the gates have not seen.
cla	Asks whether the author of a pull request has agreed to the contribution terms. Not reproducible locally by design: the answer depends on who opened the pull request and on GitHub's own view of whether they are inside this organisation, neither of which exists on a laptop. It gates a merge and nothing else.
build	Builds the runtime image for one architecture, on a runner of that architecture, and pushes it by digest for `publish` to combine. Not reproducible locally by design: it needs a registry credential and a machine of each architecture, and a local target that pushed would mean anybody could publish from a laptop.
chart	Packages the Helm chart and pushes it beside the image it names. Not reproducible locally for the same reason as `publish`: it needs a registry credential, and a local target that pushed would mean anybody could publish a chart from a laptop. It gates nothing, and runs only after `publish` has put the image it pins in the registry.
e2e-cluster-k3d	Installs the chart on a throwaway k3d cluster, against the real platform, and signs a person in: paths 1 and 2 of spec/installation-and-first-run and path 1's other two ways in. Reproducible locally and exactly, as `make e2e-cluster-k3d E2E_CLUSTER_TARGET=<one of the four>`, but not from ci-local: it needs k3d and a checkout of meridian-platform, which is private and is read with a GitHub App token, and ci-local assumes neither. It gates publication, not a merge: it runs on main after ci-local, and publish waits on the workflow it is part of.
